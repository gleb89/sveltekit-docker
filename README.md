# create-svelte docker



### Docker

```bash
# prod
docker-compose --file docker-compose.prod.yml up --build

# dev
docker-compose --file docker-compose.dev.yml up --build
```

# http://localhost:3000
